/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["we-captured.kr", "www.consortium.co.uk", "kream-phinf.pstatic.net", "www.sevenstore.com"],
    },
};

module.exports = nextConfig;
